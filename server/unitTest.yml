# This workflow runs unit tests and prints a message

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run unit tests
        run: |
          # Print a message regardless of test outcome
          echo "Tests finished!"

