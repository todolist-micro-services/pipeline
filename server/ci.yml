stages:
  - build
  - test

build-job:
  stage: build
  image: gradle:7.5.1-jdk18-alpine
  script:
    - ./gradlew build --info

unit-test-job:
  stage: test
  image: gradle:7.5.1-jdk18-alpine
  script:
    - ./gradlew test --info
